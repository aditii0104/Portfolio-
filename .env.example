# GitHub username to show on the "Live from GitHub" stats section
GITHUB_USERNAME=aditii0104

# Optional: a GitHub personal access token (no scopes needed for public data)
# Raises the API rate limit from 60/hr to 5000/hr. Keep this secret, server-side only.
GITHUB_TOKEN=
