Drop your real resume PDF in this folder, named to match `resumeHref`
in `data/profile.ts` (defaults to `Aditi-Resume.pdf`).

The "Download CV" buttons link directly to this file, so the path
must match exactly.
